name: Daily LeetCode

on:
  # 定时触发：UTC 时间每天 00:00（北京时间 08:00）
  schedule:
    - cron: '0 0 * * *'

  # 支持手动触发
  workflow_dispatch:
    inputs:
      date:
        description: '指定日期 (格式: YYYY-MM-DD，留空则使用今天)'
        required: false
        default: ''

jobs:
  fetch-daily-question:
    runs-on: ubuntu-latest

    permissions:
      contents: write  # 需要写入权限来提交更新

    steps:
      - name: 检出仓库
        uses: actions/checkout@v4

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 安装依赖
        run: npm ci

      - name: 获取每日一题
        run: node scripts/fetch-leetcode.js
        env:
          TARGET_DATE: ${{ github.event.inputs.date }}

      - name: 配置 Git
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

      - name: 提交更改
        run: |
          git add -A
          # 检查是否有更改需要提交
          if git diff --staged --quiet; then
            echo "没有更改需要提交"
          else
            git commit -m "📝 更新 LeetCode 每日一题 - $(date +'%Y-%m-%d')"
            git push
          fi
